# @mandarinko/server

Express TypeScript server package for the Mandarinko monorepo.

To run the server locally, install dependencies:

```bash
pnpm install
```

Run tests for the server package:

```bash
pnpm --filter @mandarinko/server run test
```
